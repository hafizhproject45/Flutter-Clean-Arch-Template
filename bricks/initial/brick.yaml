name: initial
description: Initial components

version: 0.1.0+1

environment:
  mason: ">=0.1.0 <1.0.0"

vars:
  project_name:
    type: string
    description: Project name
    prompt: Feature name ("e.g., todo_apps")?

  is_auth:
    type: boolean
    default: true
    description: Authentication
    prompt: Do you want to use auth (Y/n)?
