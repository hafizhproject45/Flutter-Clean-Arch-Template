name: initial
description: Initial components

version: 0.1.0+1

environment:
  mason: ">=0.1.0 <1.0.0"

vars:
  project_name:
    type: string
    description: Project name
    default: apps
    prompt: Project name ("e.g., apps")?
