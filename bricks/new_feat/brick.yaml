name: new_feat
description: Clean arch feature

version: 0.1.0

environment:
  mason: ">=0.1.0 <1.0.0"

vars:
  project_name:
    type: string
    description: Project name
    default: apps
    prompt: Feature name ("e.g., apps")?
  feat_name:
    type: string
    description: Feature name
    default: user
    prompt: Feature name (Type with singular "e.g., user")?
